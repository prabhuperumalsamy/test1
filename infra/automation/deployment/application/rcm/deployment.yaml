apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "3"
  generation: 7
  labels:
    app: rcm
    app.kubernetes.io/managed-by: pulumi
  name: rcm
  namespace: actimize
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: rcm
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: rcm
      namespace: actimize
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: "topology.kubernetes.io/zone"
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: rcm
        - maxSkew: 1
          topologyKey: "kubernetes.io/hostname"
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: rcm
      containers:
      - env:
        - name: AWS_ROLE_ARN
          value: arn:aws:iam::556277294023:role/actimize-test-k8s-pods
        - name: AWS_WEB_IDENTITY_TOKEN_FILE
          value: /run/secrets/eks.amazonaws.com/serviceaccount/token
        image: 556277294023.dkr.ecr.us-east-1.amazonaws.com/actimize-test-rcm:1ff6694d3f8d86d1023e5b3ae03b42d2c6d7217a
        imagePullPolicy: Always
        name: rcm
        ports:
        - containerPort: 8080
          name: rcm0
          protocol: TCP
        resources:
          limits:
            cpu: "4"
            memory: 16Gi
          requests:
            cpu: "4"
            memory: 8Gi
        startupProbe:
          failureThreshold: 20
          httpGet:
            path: /RCM/acegi/heartBeat.jsp
            port: 8080
            scheme: HTTP
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 1
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /opt/actone/working_dir/actimizeLogs
          name: rcm-logs
        - mountPath: /opt/actone/working_dir/outgoingForms
          name: rcm-sars
        - mountPath: /opt/tomcat/apache-tomcat-9.0.45/logs
          name: tomcat-logs
      dnsPolicy: ClusterFirst
      hostname: rcm
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      serviceAccount: actimize-test-k8s-pods
      serviceAccountName: actimize-test-k8s-pods
      terminationGracePeriodSeconds: 30
      volumes:
      - name: rcm-logs
        persistentVolumeClaim:
          claimName: rcm-logs
      - name: rcm-sars
        persistentVolumeClaim:
          claimName: rcm-sars
      - emptyDir: {}
        name: tomcat-logs
      - emptyDir: {}
        name: actone-logs
